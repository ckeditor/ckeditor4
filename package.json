{
  "name": "bender-ckeditor",
  "version": "0.0.1",
  "description": "CKEditor plugin for Bender",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "when": "~3.1.0"
  }
}
